# Spine动画系统 - 完整执行方案

**版本**: v1.0  
**日期**: 2025-11-06  
**预计完成时间**: 3周  
**状态**: 🟡 基础组件已完成，等待资源准备

---

## 📋 执行方案概览

根据 `项目开发大纲-革新版.md` 的架构设计，Spine动画系统采用**脚本驱动 + 前端渲染**的混合模式，充分利用酒馆助手的事件监听和消息通信机制。

### 核心理念

1. **脚本自动检测**: 后台脚本监听AI消息，自动检测情感并发送动画指令
2. **前端渲染**: Vue组件负责Spine动画的渲染和播放
3. **消息通信**: 使用 `postMessage` 在脚本和前端之间传递指令
4. **零侵入**: 不修改酒馆核心代码，完全使用酒馆助手API

---

## 🗂️ 文件结构

### 已完成的文件

```
E:\偶像大师\tavern_helper_template\
├── src\
│   ├── 偶像大师闪耀色彩-重构\
│   │   ├── 工具\
│   │   │   ├── spine-loader.ts              ✅ Spine资源加载器
│   │   │   └── spine-animation-manager.ts   ✅ 动画管理器
│   │   ├── 组件\
│   │   │   ├── Spine播放器.vue              ✅ Spine播放组件
│   │   │   └── Spine交互层.vue              ✅ 点击交互组件
│   │   └── 页面\
│   │       └── Spine展示.vue                 ✅ 集成展示页面
│   │
│   └── 脚本示例\
│       └── spine-controller\
│           ├── emotion-detector.ts           ✅ 情感检测模块
│           └── index.ts                      ✅ 主脚本控制器
│
└── E:\偶像大师\项目进度记录\
    ├── Spine动画系统使用文档.md              ✅ 完整使用文档
    └── Spine动画系统-执行方案.md             ✅ 本文档
```

### 待创建的文件

```
E:\偶像大师\tavern_helper_template\
└── src\
    └── 脚本示例\
        ├── background-controller\            ⏳ 背景切换脚本
        │   └── index.ts
        └── music-controller\                 ⏳ 音乐控制脚本
            └── index.ts
```

---

## 📅 三周执行计划

### 第1周：资源准备与基础测试

#### Day 1-2: Spine资源整理

**任务**:
1. 收集28个偶像的Spine资源（`.atlas`, `.json`, `.png`）
2. 统一命名规范：`{idol_id}/`
3. 确认动画名称标准化

**输出**:
```
E:\偶像大师\闪耀色彩图片-最终版\spine\
├── mano\          ✅ 樱木真乃
│   ├── mano.atlas
│   ├── mano.json
│   └── mano.png
├── hiori\         ✅ 风野灯织
├── meguru\        ✅ 八宫惠
├── kogane\        ⏳ 月冈恋钟
├── mamimi\        ⏳ 田中摩美美
├── sakuya\        ⏳ 白濑咲耶
├── yuika\         ⏳ 三峰结华
├── kiriko\        ⏳ 幽谷雾子
├── amana\         ⏳ 小宫果穗
├── tenka\         ⏳ 园田智代子
└── ... (其他18个偶像)
```

#### Day 3-4: 上传到GitHub CDN

**任务**:
1. 将所有Spine资源上传到 GitHub 仓库
2. 获取CDN URL
3. 更新 `spine-loader.ts` 中的资源映射

**脚本**:
```bash
cd "E:\偶像大师\闪耀色彩图片-最终版"
git add spine/
git commit -m "添加28个偶像的Spine资源"
git push origin main
```

**更新配置**:
```typescript
// src/偶像大师闪耀色彩-重构/工具/spine-loader.ts
export const SPINE_ASSETS_MAP: Record<string, SpineAsset> = {
  mano: { ... },
  hiori: { ... },
  meguru: { ... },
  kogane: { ... },
  // ... 其他24个偶像
};
```

#### Day 5-7: 基础功能测试

**测试清单**:

1. **Spine播放器测试**
   - [ ] 加载单个偶像资源
   - [ ] 播放待机动画
   - [ ] 播放情绪动画
   - [ ] 动画切换流畅
   - [ ] 调试面板显示正常

2. **交互层测试**
   - [ ] 点击头部触发动画
   - [ ] 点击身体触发动画
   - [ ] 点击手部触发动画
   - [ ] 点击反馈效果

3. **脚本控制器测试**
   - [ ] 监听AI消息事件
   - [ ] 情感检测准确性
   - [ ] 动画指令发送
   - [ ] 说话动画自动播放

**测试方法**:
```bash
# 编译项目
pnpm run build -- --env build=偶像大师闪耀色彩-重构
pnpm run build -- --env build=脚本示例

# 在浏览器中打开
# 1. 前端界面: dist/偶像大师闪耀色彩-重构/页面/Spine展示.html
# 2. 在酒馆助手中加载脚本: dist/脚本示例/spine-controller/index.js
```

---

### 第2周：功能完善与优化

#### Day 8-10: 情感检测优化

**任务**:
1. 根据测试结果调整关键词库
2. 优化权重和置信度阈值
3. 添加更多情感类型
4. 处理边缘情况

**优化示例**:
```typescript
// emotion-detector.ts
const EMOTION_KEYWORDS_MAP = {
  happy: {
    keywords: [
      '高兴', '开心', '快乐', '笑', '哈哈', '嘻嘻',
      // 添加更多关键词
      '太好了', '真棒', '耶', '欢呼',
    ],
    animation: 'Emotion_Happy',
    weight: 1.0,
  },
  // ... 其他情感
};
```

#### Day 11-12: 背景切换系统

**创建**:
```typescript
// src/脚本示例/background-controller/index.ts

const backgrounds = {
  '海滩': 'beach.jpg',
  '图书馆': 'library.jpg',
  '咖啡厅': 'cafe.jpg',
  '录音室': 'studio.jpg',
  '舞蹈室': 'dance_room.jpg',
  '事务所': 'office.jpg',
};

eventOn(tavern_events.MESSAGE_RECEIVED, async (messageId: number) => {
  const message = SillyTavern.chat[messageId];
  const text = message.mes;
  
  // 自动检测地点关键词
  for (const [keyword, bgImage] of Object.entries(backgrounds)) {
    if (text.includes(keyword)) {
      window.postMessage({
        type: 'CHANGE_BACKGROUND',
        payload: { background: bgImage }
      }, '*');
      break;
    }
  }
  
  // 解析 <background> 标签
  const bgMatch = text.match(/<background>(.*?)<\/background>/);
  if (bgMatch) {
    const bg = bgMatch[1];
    window.postMessage({
      type: 'CHANGE_BACKGROUND',
      payload: { background: backgrounds[bg] || 'default.jpg' }
    }, '*');
  }
});
```

#### Day 13-14: 音乐控制系统

**创建**:
```typescript
// src/脚本示例/music-controller/index.ts

const bgm_map = {
  '海滩': 'beach_bgm.mp3',
  '比赛': 'battle_bgm.mp3',
  '训练': 'training_bgm.mp3',
  '结局': 'ending_bgm.mp3',
};

eventOn(tavern_events.MESSAGE_RECEIVED, async (messageId: number) => {
  const message = SillyTavern.chat[messageId];
  const text = message.mes;
  
  for (const [keyword, bgm] of Object.entries(bgm_map)) {
    if (text.includes(keyword)) {
      await playBackgroundMusic(bgm);
      break;
    }
  }
});

function playBackgroundMusic(filename: string) {
  const audio = new Audio(`https://raw.githubusercontent.com/.../bgm/${filename}`);
  audio.loop = true;
  audio.volume = 0.3;
  audio.play();
}
```

---

### 第3周：集成测试与文档完善

#### Day 15-17: 酒馆集成测试

**测试场景**:

1. **场景1: 普通对话**
   - 用户发送消息
   - AI回复（自动情感检测）
   - Spine播放对应动画
   - 动画完成后返回待机

2. **场景2: 培育训练**
   - 用户点击"Vocal训练"
   - 打开打牌界面
   - 训练完成后AI生成剧情
   - Spine播放情感动画 + 背景切换

3. **场景3: 自由活动**
   - 用户选择"去海滩"
   - AI生成互动剧情
   - Spine播放高兴动画
   - 背景切换到海滩
   - 播放海滩BGM

4. **场景4: 点击交互**
   - 用户点击角色头部
   - Spine播放 `Touch_Head` 动画
   - AI生成回应（可选）

**测试清单**:
- [ ] 前端界面正常渲染
- [ ] 脚本事件监听正常
- [ ] 动画指令传递正常
- [ ] 多个偶像切换正常
- [ ] 背景切换流畅
- [ ] 音乐播放正常
- [ ] 内存占用合理
- [ ] 无明显卡顿

#### Day 18-19: 性能优化

**优化项**:
1. 资源预加载
2. 动画缓存
3. 内存管理
4. 渲染优化

**示例**:
```typescript
// 预加载常用偶像
const FREQUENTLY_USED_IDOLS = ['mano', 'hiori', 'meguru'];
await preloadSpineAssets(FREQUENTLY_USED_IDOLS);

// 卸载不常用资源
if (memoryUsage > THRESHOLD) {
  await unloadSpineAsset('rarely_used_idol');
}
```

#### Day 20-21: 文档完善与发布

**任务**:
1. 更新使用文档
2. 添加更多示例
3. 录制演示视频
4. 准备发布说明

**输出**:
- ✅ `Spine动画系统使用文档.md`
- ✅ `Spine动画系统-执行方案.md`
- ⏳ 演示视频
- ⏳ 发布说明

---

## 🔧 技术要点

### 1. 资源加载策略

```typescript
// 优先级加载
const loadingPriority = {
  high: ['mano', 'hiori', 'meguru'],    // 立即加载
  medium: ['kogane', 'mamimi', 'sakuya'], // 延迟加载
  low: [...],                              // 按需加载
};

// 预加载高优先级资源
await preloadSpineAssets(loadingPriority.high);

// 后台加载中优先级资源
setTimeout(() => {
  preloadSpineAssets(loadingPriority.medium);
}, 3000);
```

### 2. 动画队列管理

```typescript
// 优先级系统
const PRIORITY = {
  INTERACTION: 20,  // 交互动画最高优先级
  EMOTION: 10,      // 情绪动画中优先级
  TALK: 5,          // 说话动画低优先级
  IDLE: 0,          // 待机动画最低优先级
};

// 添加到队列时指定优先级
spineAnimationManager.addToQueue({
  name: 'Touch_Head',
  loop: false,
  priority: PRIORITY.INTERACTION,
});
```

### 3. 性能监控

```typescript
// 监控FPS
let lastTime = performance.now();
let fps = 0;

function updateFPS() {
  const now = performance.now();
  fps = 1000 / (now - lastTime);
  lastTime = now;
  
  if (fps < 30) {
    console.warn('⚠️ FPS过低，考虑降低质量');
  }
}

// 监控内存
const memoryInfo = (performance as any).memory;
if (memoryInfo) {
  console.log('内存使用:', memoryInfo.usedJSHeapSize / 1024 / 1024, 'MB');
}
```

### 4. 错误恢复

```typescript
// 加载失败时的降级方案
try {
  await loadSpineAsset(idolId);
} catch (error) {
  console.error('Spine加载失败，使用备用方案');
  
  // 方案A: 使用默认偶像
  await loadSpineAsset('mano');
  
  // 方案B: 显示静态图片
  showStaticImage(idolId);
  
  // 方案C: 隐藏Spine，仅显示文字
  hideSpineLayer();
}
```

---

## 📊 进度追踪

### 已完成 ✅

| 任务            | 状态 | 完成日期   |
| --------------- | ---- | ---------- |
| Spine资源加载器 | ✅    | 2025-11-06 |
| 动画管理器      | ✅    | 2025-11-06 |
| Spine播放器组件 | ✅    | 2025-11-06 |
| Spine交互层组件 | ✅    | 2025-11-06 |
| 情感检测模块    | ✅    | 2025-11-06 |
| 主脚本控制器    | ✅    | 2025-11-06 |
| Spine展示页面   | ✅    | 2025-11-06 |
| 使用文档        | ✅    | 2025-11-06 |

### 进行中 🔄

| 任务           | 状态 | 预计完成 |
| -------------- | ---- | -------- |
| Spine资源准备  | 🔄    | Week 1   |
| GitHub CDN上传 | 🔄    | Week 1   |
| 基础功能测试   | 🔄    | Week 1   |

### 待开始 ⏳

| 任务         | 优先级 | 预计时间 |
| ------------ | ------ | -------- |
| 情感检测优化 | 高     | 3天      |
| 背景切换系统 | 中     | 2天      |
| 音乐控制系统 | 中     | 2天      |
| 酒馆集成测试 | 高     | 3天      |
| 性能优化     | 中     | 2天      |
| 文档完善     | 低     | 2天      |

---

## 🎯 成功标准

### 功能完整性

- [x] Spine资源成功加载
- [x] 基础动画播放正常
- [x] 情感自动检测
- [x] 交互反馈
- [ ] 28个偶像全部可用
- [ ] 背景切换功能
- [ ] 音乐播放功能

### 性能指标

- [ ] FPS ≥ 30
- [ ] 加载时间 < 3秒
- [ ] 内存占用 < 200MB
- [ ] 动画切换流畅（< 100ms）

### 用户体验

- [ ] 界面美观易用
- [ ] 错误提示友好
- [ ] 调试工具完善
- [ ] 文档清晰详细

---

## 📞 联系与支持

### 相关文档

- `项目开发大纲-革新版.md` - 整体架构设计
- `Spine动画系统使用文档.md` - 详细使用指南
- `@types/function/*.d.ts` - 酒馆助手API定义

### 调试工具

```javascript
// 浏览器控制台
window.__spineController.testEmotion('测试文本');
window.__spineController.playEmotion('Emotion_Happy');
```

### 常见问题

参考 `Spine动画系统使用文档.md` 的"常见问题"章节。

---

**执行方案版本**: v1.0  
**最后更新**: 2025-11-06  
**下次审查**: Week 1 结束后  
**状态**: 🟡 进行中



